# Руководство по оптимизации размера файлов записи экрана

## Обзор оптимизаций

Файл `screen_recorder.dart` был оптимизирован для уменьшения размера выходных файлов. Реализованы следующие улучшения:

### 1. Оптимизированные значения по умолчанию

**До оптимизации:**
- `pixelRatio`: 0.5
- `resizeRatio`: 0.7 (70% от оригинала)
- `jpegQuality`: 88%

**После оптимизации:**
- `pixelRatio`: 0.3 (уменьшено с 0.5)
- `resizeRatio`: 0.5 (50% от оригинала, уменьшено с 0.7)
- `jpegQuality`: 75 (уменьшено с 88)

### 2. Настраиваемые параметры

Теперь все параметры сжатия можно настроить при создании `ScreenRecorderController`:

```dart
final controller = ScreenRecorderController(
  pixelRatio: 0.3,           // Разрешение захвата (0.3-0.7)
  resizeRatio: 0.5,           // Коэффициент ресайза (0.3-1.0)
  jpegQuality: 75,            // Качество JPEG (1-100)
  skipFramesBetweenCaptures: 2, // Пропуск кадров
);
```

## Параметры оптимизации

### pixelRatio (разрешение захвата)
- **Диапазон**: 0.2 - 1.0
- **По умолчанию**: 0.3
- **Влияние**: 
  - Меньше значение = меньше размер файла, но ниже качество
  - 0.3 = минимальный размер (рекомендуется для экономии места)
  - 0.5 = баланс качества и размера
  - 0.7+ = высокое качество, но большой размер

### resizeRatio (коэффициент ресайза)
- **Диапазон**: 0.3 - 1.0
- **По умолчанию**: 0.5
- **Влияние**:
  - Уменьшает размер изображения перед JPEG сжатием
  - 0.5 = уменьшение до 50% от оригинала
  - 0.3 = максимальное сжатие (может ухудшить читаемость текста)
  - 0.7 = хороший баланс

### jpegQuality (качество JPEG)
- **Диапазон**: 1 - 100
- **По умолчанию**: 75
- **Влияние**:
  - Меньше значение = меньше размер файла
  - 60-70 = минимальный размер (может быть заметна потеря качества)
  - 75-80 = хороший баланс (рекомендуется)
  - 85-95 = высокое качество, но большой размер

### skipFramesBetweenCaptures (пропуск кадров)
- **Диапазон**: 1 - 10+
- **По умолчанию**: 2
- **Влияние**:
  - Больше значение = меньше кадров = меньше размер файла
  - Может сделать видео менее плавным

## Рекомендуемые профили

### 🎯 Минимальный размер (максимальная оптимизация)
```dart
ScreenRecorderController(
  pixelRatio: 0.3,
  resizeRatio: 0.4,
  jpegQuality: 65,
  skipFramesBetweenCaptures: 3,
)
```
**Ожидаемое уменьшение**: ~70-80% от исходного размера
**Качество**: Приемлемое для большинства случаев

### ⚖️ Сбалансированный (по умолчанию)
```dart
ScreenRecorderController(
  pixelRatio: 0.3,
  resizeRatio: 0.5,
  jpegQuality: 75,
  skipFramesBetweenCaptures: 2,
)
```
**Ожидаемое уменьшение**: ~50-60% от исходного размера
**Качество**: Хорошее, подходит для большинства задач

### 🎨 Высокое качество
```dart
ScreenRecorderController(
  pixelRatio: 0.5,
  resizeRatio: 0.7,
  jpegQuality: 85,
  skipFramesBetweenCaptures: 1,
)
```
**Ожидаемое уменьшение**: ~30-40% от исходного размера
**Качество**: Отличное, минимальная потеря деталей

## Пример использования

```dart
// В screen_recorder_wrapper.dart или main.dart
ScreenRecorderWrapper(
  pixelRatio: 0.3,              // Оптимизированное разрешение
  skipFramesBetweenCaptures: 2,
  child: YourWidget(),
)

// Или с кастомными параметрами сжатия
final controller = ScreenRecorderController(
  pixelRatio: 0.3,
  resizeRatio: 0.4,              // Дополнительное уменьшение
  jpegQuality: 70,               // Более агрессивное сжатие
  skipFramesBetweenCaptures: 3,
);
```

## Ожидаемые результаты

При использовании оптимизированных значений по умолчанию:
- **Размер файла**: уменьшение на 50-70% по сравнению с предыдущими настройками
- **Качество**: приемлемое для большинства случаев использования
- **Производительность**: улучшена за счет меньшего объема данных

## Дополнительные советы

1. **Для записи текста**: используйте `jpegQuality: 80+` и `resizeRatio: 0.6+`
2. **Для записи UI/анимаций**: можно использовать более агрессивные настройки
3. **Для длинных записей**: увеличьте `skipFramesBetweenCaptures` до 3-5
4. **Тестирование**: попробуйте разные комбинации и выберите оптимальный баланс для вашего случая

## Технические детали

Оптимизация работает в два этапа:
1. **Захват**: `pixelRatio` определяет разрешение при захвате кадра
2. **Сжатие**: `resizeRatio` уменьшает размер, затем `jpegQuality` применяет JPEG сжатие

Оба этапа работают вместе для максимального уменьшения размера файла.
